; WP-L5 call-expression candidates. `attribute`-vs-`identifier` splitting
; of the callee (receiver/name), mirroring PythonASTExtractor.visit_Call,
; happens in Python since it needs field-level inspection of `function`.
(call) @node
