; WP-L4 import candidates: `import` declarations, including `import
; static` and wildcard (`import pkg.*;`) shapes — classified in Python
; since none of these need field-level filtering at the query stage.
(import_declaration) @node
