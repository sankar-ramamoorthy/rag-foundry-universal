; WP-L3 import candidates: `use` declarations. Grouped/aliased/wildcard
; shapes are all captured as the same top-level use_declaration node;
; RustExtractor walks the `argument` field's subtree in Python.
(use_declaration) @node
